apiVersion: apps/v1
kind: Deployment
metadata:
  name: joyful-server
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: joyful-server
  template:
    metadata:
      labels:
        app: joyful-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: joyful
        image: docker.korshakov.com/joyful-server:{version}
        ports:
        - containerPort: 3005
        env:
        - name: NODE_ENV
          value: production
        - name: PORT
          value: "3005"
        - name: REDIS_URL
          value: redis://joyful-redis:6379
        - name: S3_PUBLIC_URL
          value: https://files.cluster-fluster.com/joyful
        envFrom:
        - secretRef:
            name: joyful-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: joyful-server-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: joyful-server
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: joyful-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: joyful-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /joyful-db
    - extract:
        key: /joyful-master
    - extract:
        key: /joyful-github
    - extract:
        key: /joyful-files
    - extract:
        key: /joyful-e2b
    - extract:
        key: /joyful-revenuecat
    - extract:
        key: /joyful-elevenlabs
---
apiVersion: v1
kind: Service
metadata:
  name: joyful-server
spec:
  selector:
    app: joyful-server
  ports:
  - port: 3000
    targetPort: 3005
  type: ClusterIP